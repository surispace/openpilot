# Canonical NRDR sections merged into the Sunnylink host page.
page_id: steering
after_section: lane_change
sections:
  - id: nrdr
    title: nrdr Lateral Tuning
    description: Lateral tuning specific to the nrdr fork.
    items:
    - key: NrdrHandcraftedLateralTune
      widget: toggle
      title: Apply Handcrafted Lateral Profile
      description: Flip ON to request the September 12 Civic-derived preset once for this car and controller. Steer ratio, vehicle learning, and calibration stay unchanged.
      details: Common lane-centering and live-delay settings apply across supported cars; Honda-specific groups apply only to compatible controllers. A fresh request is bound to the detected vehicle and preset version. Wait a few seconds, then refresh; the switch turns OFF after verified completion. This is opt-in, not a mode; existing customizations are not automatically overwritten, and later edits persist until you deliberately apply again. The source tune was recorded on a Civic, not validated on every vehicle.
      visibility:
      - {type: capability, field: has_handcrafted_lateral_profile, equals: true}
      enablement:
      - $ref: '#/macros/handcrafted_apply_offroad'
    sub_panels:
    - id: nrdr_vehicle_model_learning
      label: Vehicle Model & Learning
      trigger_key: null
      items:
      - key: NrdrRemoteTuneScan
        widget: toggle
        title: Run Tune Report Scan
        description: Flip ON to scan every drive log into a tune report (the device flips it back OFF on the next start).
      - key: NrdrCarTuneInfo
        widget: info
        title: Loaded Profile
        details: Live vehicle, controller, base-tune, geometry, and tuning-knob details are written here by the device.
      - key: NrdrCarControllerInfo
        widget: info
        title: Controller
      - key: NrdrCarHandcraftedInfo
        widget: info
        title: Handcrafted
      - key: NrdrCarPidLowInfo
        widget: info
        title: PID Low
      - key: NrdrCarPidMidInfo
        widget: info
        title: PID Mid
      - key: NrdrCarPidHighInfo
        widget: info
        title: PID Hwy
      - key: NrdrCarDampingInfo
        widget: info
        title: Damping
      - key: NrdrCarCenterInfo
        widget: info
        title: Center
      - key: NrdrCarNnlcInfo
        widget: info
        title: NNLC
      - key: NrdrCarSteerRatioInfo
        widget: info
        title: Steer Ratio
      - key: NrdrCarLearningInfo
        widget: info
        title: Learning
      - key: NrdrCarHelpersInfo
        widget: info
        title: Helpers
      - key: NrdrLearnStiffness
        widget: toggle
        title: Learn Tire Stiffness (Auto)
        description: When ON (Auto), uses the live-learned tire stiffness factor. When OFF, uses the static base value of 1.0.
      - key: NrdrLearnAngleOffset
        widget: toggle
        title: Learn Angle Offset (Auto)
        description: When ON (Auto), uses the live-learned steering angle offset; OFF uses a static 0.0 offset.
        details: When ON (Auto), uses the live-learned steering angle offset. When OFF, uses a static 0.0 offset. Turn OFF if a bad learned offset is pulling the car to one side.
    - id: nrdr_pidf_ground
      label: Controller Tuning Dungeon
      trigger_key: null
      trigger_condition: {type: capability, field: nrdr_honda_tuning_available, equals: true}
      items:
      - key: NrdrInterpolatedTorquePifBlend
        widget: toggle
        title: Interpolated Torque/PIF Blend
        description: Blend angle-feedback P/I/F with the generic f13 legacy torque controller.
        details: The final request is Torque share times the f13de17 generic torque controller plus the remaining share times today's complete P/I/F controller. Torque uses angle feedback through 2 m/s, transitions from angle to calibrated yaw over 2-5 m/s, and uses calibrated yaw at 5 m/s and above. If required yaw is invalid, stale, missing, or non-finite, Torque does not reuse angle or update its controller state; the final request temporarily returns to 100% P/I/F. This generic yaw branch was not historically road-proven on Honda. All six settings apply live after the next background refresh (normally within 0.5 seconds), with a one-second torque transition. No LKAS cycle is required. Have a passenger make adjustments while driving. Clarity NNLC is bypassed and reset.
        enablement:
        - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
        sub_items:
        - key: NrdrInterpolatedTorqueShare
          widget: option
          title: Torque / P/I/F Share
          description: Splits the final steering request between both complete controllers. Torque X% / P/I/F (100-X)%; default is 50% / 50%.
          details: The two shares always add to 100%. Changes apply from one live six-setting snapshot without disengagement.
          min: 0
          max: 100
          step: 1
          unit: '%'
          enablement:
          - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
          - {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrInterpolatedTorqueLatAccelFactor
          widget: option
          enablement:
          - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
          - {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrInterpolatedTorqueFriction
          widget: option
          enablement:
          - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
          - {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrInterpolatedTorqueFrictionStandard
          widget: option
          enablement:
          - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
          - {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrInterpolatedTorqueFrictionHighway
          widget: option
          enablement:
          - {type: capability, field: nrdr_interpolated_torque_pif_blend_available, equals: true}
          - {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
      - key: NrdrStarPilotPid
        widget: toggle
        title: StarPilot PID Additions
        description: Adds the borrowed StarPilot turn-in / unwind / per-direction output scaling, not built for Honda.
        details: The borrowed turn-in / unwind / per-direction output scaling (the StarPilot half of _pid_output_scale) not built for Honda. Off = clean banded PID/F + rate-damping D, your center boost kept. Turn on to A/B it against the raw base.
      - key: LatPScaleLowSpeed
        widget: option
      - key: LatIScaleLowSpeed
        widget: option
      - key: LatFScaleLowSpeed
        widget: option
        title: Low Speed Feedforward Scale (Below 25mph)
        description: Scales the feedforward (kf) term below 25 mph (100% = tuned value).
        details: Scales the feedforward (kf) term below 25 mph. Follows the commanded angle rather than error, adding authority without amplifying noise. 100% = tuned value (static).
        min: 0
        max: 500
        step: 5
        unit: '%'
      - key: LatPScaleStandard
        widget: option
      - key: LatIScaleStandard
        widget: option
      - key: LatFScaleStandard
        widget: option
        title: Standard Speed Feedforward Scale (25-50mph)
        description: Scales the feedforward (kf) term between 25 and 50 mph (100% = tuned value).
        min: 0
        max: 500
        step: 5
        unit: '%'
      - key: LatPScaleHighway
        widget: option
      - key: LatIScaleHighway
        widget: option
      - key: LatFScaleHighway
        widget: option
        title: Highway Feedforward Scale (50mph+)
        description: Scales the feedforward (kf) term above 50 mph (100% = tuned value).
        min: 0
        max: 500
        step: 5
        unit: '%'
      - key: NrdrLatRateDamping
        widget: option
        title: Rate Damping (D) Strength
        description: Adds a damping (D) term opposing wheel speed to flatten low-speed oscillation; 0 = off.
        details: The derivative term openpilot normally can't use. Adds torque opposing how fast the wheel is moving, which damps the low-speed oscillation plain P can't (the wheel is torque-commanded, so P alone rings). Relies on a clean steering-rate signal and a low-lag EPS - which this car has. 0 = off. Raise until the low-speed wobble flattens; too high makes turn-in feel heavy.
        min: 0
        max: 300
        step: 5
        unit: '%'
      - key: NrdrLatRateDampingFadeSpeed
        widget: option
        title: Rate Damping Fade-Out Speed
        description: Speed at which rate damping tapers to zero (strongest at a standstill).
        details: Speed at which rate damping tapers to zero. Damping is strongest at a standstill and gone by this speed, where tire self-aligning torque resumes damping the steering itself. Keeps the D term out of your highway feel.
        min: 0
        max: 60
        step: 1
        unit: mph
      - key: HondaCenterScale
        widget: option
        title: Center Boost
        description: Extra proportional (P) error correction near center; 0.50 is a 50% P boost.
        details: Multiplies only the proportional term near center. Integral, feedforward, and damping keep their normal values. Available at every speed above the minimum, including the highway.
        min: 0.0
        max: 5.0
        step: 0.01
      - key: HondaCenterBoostThreshold
        widget: option
        title: Center Boost Threshold (deg)
        description: How centered should the steering wheel be when center boost is actually active?
        min: 0.0
        max: 10.0
        step: 0.1
        unit: deg
      - key: HondaCenterBoostMinSpeed
        widget: option
        title: Center Boost Minimum Speed
        description: Below this speed center boost is disabled; there is no upper-speed cutoff.
        details: Below this speed center boost is disabled. Above the short ramp it remains fully available, including on the highway.
        min: 0
        max: 80
        step: 1
        unit: mph
      - key: NrdrLatStiction
        widget: toggle
        title: Predictive Lateral Stiction
        description: Tapers torque before a stable steering target, then holds it until another correction is needed.
        details: PID remains the normal throw. A predictive capture stage can only reduce torque as the wheel closes on a stable target, then freezes the captured output until error, relative motion, drift, or a decisive corrective torque reversal requires reacquisition. It operates at every speed and immediately bypasses for driver input, lane changes, steering faults, or control limits. Default is OFF.
      - key: NrdrNnlcEnabled
        widget: toggle
        enablement:
        - type: not
          condition: {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        title: Enable Neural Network Lateral Model (NNLC)
        description: Use PID at lower speeds and during lane changes, then smoothly hand off to the Clarity neural lateral model at higher speeds. Default is OFF.
        details: OFF keeps the exact Clarity interface.py PID tune active everywhere. ON runs the PID/NNLC hybrid; lane changes immediately select PID before their opening torque command. NNLC remains configured but is inactive whenever a recognized Clarity is using the experimental firmware EPS position map or Interpolated Torque/PIF Blend.
        sub_items:
        - key: NrdrNnlcActivationSpeed
          widget: option
          title: Activate NNLC Above
          description: Center speed of the smooth 6 mph PID-to-NNLC handoff. Default is 30 mph (PID full through 27 mph, NNLC full from 33 mph).
          min: 0
          max: 100
          step: 1
          unit: mph
          enablement:
          - type: param
            key: NrdrNnlcEnabled
            equals: true
          - type: not
            condition: {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrNnlcKpGain
          widget: option
          title: NNLC KP Gain Scale
          description: NNLC proportional feedback gain. 100% gives kp = 1.0.
          min: 0
          max: 300
          step: 5
          unit: '%'
          enablement:
          - type: param
            key: NrdrNnlcEnabled
            equals: true
          - type: not
            condition: {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrNnlcKfGain
          widget: option
          title: NNLC KF Gain Scale
          description: NNLC neural-model feedforward gain. 50% gives kf = 0.5.
          min: 0
          max: 300
          step: 5
          unit: '%'
          enablement:
          - type: param
            key: NrdrNnlcEnabled
            equals: true
          - type: not
            condition: {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
        - key: NrdrNnlcKiGain
          widget: option
          title: NNLC KI Gain Scale
          description: NNLC integral feedback gain. 10% gives ki = 0.1.
          min: 0
          max: 300
          step: 5
          unit: '%'
          enablement:
          - type: param
            key: NrdrNnlcEnabled
            equals: true
          - type: not
            condition: {type: param, key: NrdrInterpolatedTorquePifBlend, equals: true}
    - id: nrdr_lane_centering_stack
      label: Lane Centering Stack
      trigger_key: null
      items:
      - key: LaneCentering
        widget: toggle
        title: Enable Lane Centering
        description: Disabled by default. Development-only, delay-aware lane centering; not road-validated. Settings apply live without disengagement.
        details: Builds a lane-center path from two confident boundaries and compares it with the model after the steering action takes effect. Uses timing carried with the model action; no separate timing message or setting is needed. Looks farther ahead as delay increases and requires enough valid road preview. Missing timing, timing over 0.875 seconds, or short preview fades back to the model; TIME and SHORT explain these states. Checks the corridor farther ahead for diverging markings and reduces authority as confidence falls. When the paths agree, it requests no correction. The combined model and lane command still passes through the normal curvature and jerk limits.
      - key: LaneCenteringMinSpeed
        widget: option
        title: Minimum Lane Centering Speed
        description: Arms at this speed and releases smoothly 3 mph below it, bounded by the 5 m/s controller floor.
        details: Existing enabled installs adopt the new 50 mph minimum after updating, which only removes lower-speed authority.
        min: 12
        max: 100
        step: 1
        unit: mph
        enablement:
        - {type: param, key: LaneCentering, equals: true}
      - key: LaneCenteringPauseOnSignal
        widget: toggle
        title: Fade on Turn Signal
        description: Fade out the correction while a turn signal is active. Lane-change maneuvers always suspend the correction immediately, regardless of this setting.
        enablement:
        - {type: param, key: LaneCentering, equals: true}
      - key: LaneCenterOffset
        widget: option
        title: Center Offset
        description: Shift the target left or right of the detected lane center. The controller reduces the offset in a narrow lane.
        min: -0.3
        max: 0.3
        step: 0.01
        unit: meters
        enablement:
        - {type: param, key: LaneCentering, equals: true}
      - key: LaneCenteringStrength
        widget: option
        title: Lane-Centering Strength
        description: Blend the model path toward the lane-center path. 0.0 uses the model path; 1.0 requests the lane-center path, without raising the existing maximum correction ceiling. 0.30 is the default.
        details: Confidence and steering limits always apply. Model Break-In can reduce the lane contribution during a large, confident path disagreement; set Model Break-In to 0.0 if you want no such reduction. This is a path weight, not a promise of exact physical lane position.
        min: 0.0
        max: 1.0
        step: 0.05
        enablement:
        - {type: param, key: LaneCentering, equals: true}
      - key: LaneCenteringE2EAuthority
        widget: option
        title: Model Break-In
        description: When a confident model path disagrees with lane center by more than 0.15 m across the preview, allow it to reduce the correction. At 1.0, the correction can reach zero by 0.50 m of disagreement; 0.0 never reduces it.
        details: Uses weighted disagreement over the full preview rather than one distant point. This conditional model break-in is separate from lane-center strength.
        min: 0.0
        max: 1.0
        step: 0.05
        enablement:
        - {type: param, key: LaneCentering, equals: true}
    - id: nrdr_steer_ratio_tuning
      label: Steer Ratio Tuning
      trigger_key: null
      trigger_condition: {type: capability, field: nrdr_honda_tuning_available, equals: true}
      items:
      - key: NrdrSteerRatioHybrid
        widget: toggle
        title: Enable Hybrid Steer Ratio
        description: Use source A near center, then blend into source B across 5 degrees. OFF preserves single-source operation and your existing selection.
        details: Experimental geometry, not road-validated. Configure while parked. The slider is the start of the transition, measured from straight ahead in either direction. Both sources must match this vehicle. A blend that reverses the angle-to-curvature mapping is rejected; runtime holds the last accepted geometry while active, or uses stock fallback at startup. Inspect the current steer-ratio result on the device. No PID gains or original curve data are changed.
      - key: NrdrSteerRatioMode
        widget: multiple_button
        title: Ratio A — Near Center / Single Source
        description: The only source with Hybrid OFF; the near-center source with Hybrid ON.
        details: Comma uses its last valid learned ratio as one number and holds it through brief message dropouts. NRDR Measured Curve uses fixed logged data; it does not learn while you drive. Clarity retains its audited 435.7-degree near-lock anchor. Civic TEG-A010 uses provisional v0, with data-derived anchors at 3.3-58.7 degrees and linear interpolation between them. Outside that range the nearest anchor is held as an unmeasured estimate. This Civic draft is not road-validated and has no fitted speed correction. A disabled NRDR option means no matching car/EPS data profile. Firmware uses an exact recognized EPS shape anchored to the car's stock center ratio. Settings are captured from one live snapshot. Background polling has a 0.5-second cycle, but this does not prove Sunnylink delivery or active-controller consumption. Configure experimental settings while parked. Existing settings are not changed automatically.
        options:
        - value: 0
          label: Manual
          enablement:
          - {type: capability, field: nrdr_manual_steer_ratio_available, equals: true}
        - value: 1
          label: Comma Learner
        - value: 2
          label: NRDR Measured Curve
          enablement:
          - {type: capability, field: nrdr_raw_steer_ratio_available, equals: true}
        - value: 3
          label: Firmware
          enablement:
          - {type: capability, field: nrdr_firmware_steer_ratio_available, equals: true}
      - key: NrdrSteerRatioBlendStart
        widget: option
        title: Blend Starts At
        description: Steering-wheel angle in either direction. Source B takes over fully 5 degrees later; for example, 25 degrees blends from 25 to 30 degrees. This is not a recommended tuning point.
        min: 0.0
        max: 180.0
        step: 1.0
        unit: °
        enablement:
        - {type: param, key: NrdrSteerRatioHybrid, equals: true}
      - key: NrdrSteerRatioSourceB
        widget: multiple_button
        title: Ratio B — Beyond the Blend
        description: Used after the 5-degree transition. The same transition is followed in reverse when unwinding. No speed-based switch.
        enablement:
        - {type: param, key: NrdrSteerRatioHybrid, equals: true}
        options:
        - value: 0
          label: Manual
          enablement:
          - {type: capability, field: nrdr_manual_steer_ratio_available, equals: true}
        - value: 1
          label: Comma Learner
        - value: 2
          label: NRDR Measured Curve
          enablement:
          - {type: capability, field: nrdr_raw_steer_ratio_available, equals: true}
        - value: 3
          label: Firmware
          enablement:
          - {type: capability, field: nrdr_firmware_steer_ratio_available, equals: true}
      - key: NrdrSteerRatioManualCenter
        widget: option
        title: Manual Override On-Center Ratio
        description: Ratio used with the wheel straight ahead. A bigger number asks for more steering-wheel movement for the same planned curve; a smaller number asks for less. Default 15.38.
        min: 8.0
        max: 25.0
        step: 0.01
        enablement:
        - type: any
          conditions:
          - {type: param, key: NrdrSteerRatioMode, equals: 0}
          - type: all
            conditions:
            - {type: param, key: NrdrSteerRatioHybrid, equals: true}
            - {type: param, key: NrdrSteerRatioSourceB, equals: 0}
        - {type: capability, field: nrdr_manual_steer_ratio_available, equals: true}
      - key: NrdrSteerRatioManualFinal
        widget: option
        title: Manual Override Final Ratio
        description: Ratio used near the end of wheel travel. Default 10.93.
        details: NRDR blends smoothly from the on-center number to this final number using hidden car-specific outer-angle metadata. Cars without exact metadata keep their stock ratio instead.
        min: 8.0
        max: 25.0
        step: 0.01
        enablement:
        - type: any
          conditions:
          - {type: param, key: NrdrSteerRatioMode, equals: 0}
          - type: all
            conditions:
            - {type: param, key: NrdrSteerRatioHybrid, equals: true}
            - {type: param, key: NrdrSteerRatioSourceB, equals: 0}
        - {type: capability, field: nrdr_manual_steer_ratio_available, equals: true}
    - id: nrdr_override
      label: Override Tuning
      trigger_key: null
      trigger_condition: {type: capability, field: nrdr_honda_tuning_available, equals: true}
      items:
      - key: NrdrIncreaseOverrideTolerance
        widget: toggle
        title: Increase Driver Override Hysteresis
        description: Reduces false driver override detections on sensitive Honda EPS by doubling the override threshold below.
        details: Reduces the likelihood of false driver override detections (resulting in dropped torque) on sensitive Honda EPS platforms by doubling the override threshold below.
      - key: NrdrDriverOverrideThreshold
        widget: option
        title: Driver Override Threshold
        description: Raw steering torque reading above which you count as steering (override); default 1400.
        details: Raw steering torque sensor reading above which you are considered to be steering (driver override). On cars with a different stock threshold, this value is applied proportionally so 1200 still represents the stock threshold.
        min: 100
        max: 5000
        step: 100
      - key: NrdrOverrideThresholdCenterBoost
        widget: option
        title: Override Threshold Center Boost
        description: Lower override threshold used within the Center Boost band; set equal to Driver Override Threshold to disable.
        details: When the wheel is within the Center Boost degree band (on a straight), this lower override threshold applies instead of the one above - so you can override easily on straights while curves keep the higher threshold and do not drop torque from a false override. Set equal to Driver Override Threshold to disable.
        min: 100
        max: 5000
        step: 100
      - key: HondaDriverAssistDuringOverride
        widget: toggle
        title: Pass-through assist torque on override
        description: Resets the EPS lane-assist state so the final override feels like normal driving after fade completes.
        details: When enabled, openpilot resets the EPS internal state which determines whether it is in lane assist mode or not. As such, enabling this will make the final override feel exactly the same as normal driving (after fade is completed), and when disabled the steering rack may carry on a more resistive feeling.
      - key: HondaOverrideFadeDownSecs
        widget: option
        title: Override Torque Fade Down
        description: Controls how quickly steering torque fades out when driver override begins.
        min: 0.0
        max: 10.0
        step: 0.1
        unit: second
      - key: HondaOverrideFadeUpSecs
        widget: option
        title: Override Torque Fade Up
        description: Controls how quickly steering torque fades back in after driver override ends.
        min: 0.0
        max: 10.0
        step: 0.1
        unit: second
      - key: HondaOverrideHoldSecs
        widget: option
        title: Override Hold Time Before Re-Engaging
        description: After you release the wheel, openpilot waits this long before gradually taking steering control back.
        details: Prevents steering vibrations from openpilot fighting your hand when you briefly steer (override) while it is active. 0 disables the wait and re-engages immediately as before.
        min: 0.0
        max: 10.0
        step: 0.1
        unit: second
      - key: HondaOverrideTorqueScale
        widget: option
        title: Override Torque Retain
        description: Controls how much openpilot steering torque remains when the override has gone past Override Torque Fade Down.
        min: 0
        max: 100
        step: 1
        unit: '%'
    - id: nrdr_steer_filters
      label: Steer Filters
      trigger_key: null
      trigger_condition: {type: capability, field: nrdr_honda_tuning_available, equals: true}
      items:
      - key: HondaTorqueLowPassFilter
        widget: toggle
        title: Low Pass Filter (tau)
        description: Smooths steering response at the cost of adding lag; tune to the least amount before oscillation.
        details: Filter that smooths steering response at the cost of adding lag. Tune to the least amount possible before oscillations begin. Live tests show that going above 0.1 tau rapidly increases the possibility of lag-induced oscillations.
      - key: HondaLpfTauLowSpeed
        widget: option
        title: Low Pass Filter Tau (Below 25mph)
        description: Low-pass filter time constant (seconds) below 25 mph.
        min: 0.0
        max: 5.0
        step: 0.01
        enablement:
        - type: param
          key: HondaTorqueLowPassFilter
          equals: true
      - key: HondaLpfTauStandard
        widget: option
        title: Standard Tau (25-50mph)
        description: Low-pass filter time constant (seconds) between 25 and 50 mph.
        min: 0.0
        max: 5.0
        step: 0.01
        enablement:
        - type: param
          key: HondaTorqueLowPassFilter
          equals: true
      - key: HondaLpfTauHighway
        widget: option
        title: Highway Tau (50mph+)
        description: Low-pass filter time constant (seconds) at or above 50 mph.
        min: 0.0
        max: 5.0
        step: 0.01
        enablement:
        - type: param
          key: HondaTorqueLowPassFilter
          equals: true
      - key: HondaSteerDeltaLimiter
        widget: toggle
        title: Legacy Steer Delta Rate Limiter
        description: Limits how quickly requested steering torque can rise or fall.
        details: Limits how quickly requested steering torque can rise or fall. This is an older system of limiting and may no longer prove useful compared to the low pass filter.
      - key: HondaSteerDeltaUp
        widget: option
        title: Steer Delta Up
        description: Maximum upward steering torque rate when the rate limiter is enabled (too low will add lag).
        min: 0.0
        max: 100.0
        step: 0.1
        enablement:
        - type: param
          key: HondaSteerDeltaLimiter
          equals: true
      - key: HondaSteerDeltaDown
        widget: option
        title: Steer Delta Down
        description: Maximum downward steering torque rate when the rate limiter is enabled (too low will add lag).
        min: 0.0
        max: 100.0
        step: 0.1
        enablement:
        - type: param
          key: HondaSteerDeltaLimiter
          equals: true
  - id: nrdr_special
    title: nrdr Special
    description: Dashboard, diagnostics, and special features.
    visibility:
    - {type: capability, field: nrdr_honda_tuning_available, equals: true}
    items: []
    sub_panels:
    - id: nrdr_party_tricks
      label: Special
      trigger_key: null
      items:
      - key: HondaInjectionTest
        widget: toggle
        title: Injection Test (Caution!)
        description: Multiplies the lateral PID output scale by 999% as a diagnostic stress test. Use with extreme caution.
        details: When enabled, the lateral PID output scale is multiplied by 999%. This is a diagnostic stress test to see how the car reacts to a massive steering command. Use with extreme caution on a safe, empty road.
      - key: HondaAltDashboardSpeed
        widget: multiple_button
        title: Alternative Dashboard Speed Design
        description: Repurposes the cluster's set-speed slot to show lead, GPS, or true cluster speed (needs openpilot longitudinal).
        details: Lead Speed shows the lead's speed in whole mph ("Stopped" below 1 mph, "--" with no lead). GPS Speed shows the comma's own true speed. Cluster Speed shows exactly what the dash cluster reads.
        options:
        - value: 0
          label: Stock
        - value: 1
          label: Lead Speed
        - value: 2
          label: GPS Speed
        - value: 3
          label: Cluster Speed
      - key: HondaAltDashboardDistance
        widget: option
        title: Alternative Dashboard Distance Design
        description: Repurposes the cluster's distance bars / mini car (requires openpilot longitudinal).
        details: Repurposes the cluster's distance bars / mini car (requires openpilot longitudinal). Radar closes the bars in as the lead approaches. Velocity pushes the bars out under acceleration and pulls them in under braking. Non-stock designs stay on the cluster permanently, even when not engaged.
        options:
        - value: 0
          label: Stock
        - value: 1
          label: Radar
        - value: 2
          label: Velocity
      - key: NrdrClearDashFaults
        widget: toggle
        title: Clear Dashboard Fault Codes
        description: Forces the cluster's FCM/icon fault bits off and suppresses the stock FCW chime.
        details: Forces the cluster's FCM/icon fault bits off and suppresses the stock FCW chime. Lets a car with a dead or absent stock camera run a clean dash. Turn OFF for stock openpilot behavior (camera values passed through, FCW chime active).
      - key: HondaSpoofCameraMessages
        widget: toggle
        title: Spoof Camera Messages
        description: Dead camera only - keeps the CAMERA_MESSAGES broadcast alive so the cluster never raises a high-beam fault.
        details: Dead camera only - keeps the camera's CAMERA_MESSAGES broadcast alive so the cluster never raises "Auto High Beam System Problem" (that fault is a message timeout, not a code). Leave OFF if your stock camera works.
      - key: NrdrCruiseButtonSubMode
        widget: toggle
        title: Cruise Button Sub-Mode
        description: Dynamic HUD - the distance or set/resume button first wakes a blinking preview on the cluster.
        details: Dynamic HUD - pressing the distance button or set/resume first wakes a preview on the cluster (current personality on the bars, set speed if engaged, all blinking). Only presses made during the preview actually change the personality or set speed. The blink starts lazy and accelerates continuously until it cuts off.
      - key: NrdrCruiseButtonSubModeSecs
        widget: option
        title: Sub-Mode Visibility Time
        description: How long the preview stays on the cluster after a button press.
        details: How long the preview stays on the cluster after a button press. The blink ramp always spans the whole window, slow at the start and rapid right before it falls off.
        min: 5
        max: 60
        step: 1
        unit: second
        enablement:
        - type: param
          key: NrdrCruiseButtonSubMode
          equals: true
